<!-- res/drawable/outline_button.xml -->
<shape xmlns:android="http://schemas.android.com/apk/res/android"
    android:shape="rectangle">
    <solid android:color="@android:color/transparent" /> <!-- Latar belakang transparan -->
    <stroke android:width="2dp" android:color="@color/black" /> <!-- Garis tepi berwarna hitam -->
    <corners android:radius="4dp"/> <!-- Sudut tombol yang melengkung -->
</shape>
